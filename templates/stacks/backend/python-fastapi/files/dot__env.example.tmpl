# Backend environment. Copy to `.env` for local runs (compose injects these directly).
DATABASE_URL=postgresql+asyncpg://{{ db_user }}:{{ db_password }}@{{ db_host }}:{{ db_port }}/{{ db_name }}
CORS_ORIGINS=http://localhost:{{ web_port }},http://localhost:3000
