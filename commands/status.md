---
description: Show claude-kit working memory and installed-config status for this project
allowed-tools: Skill
---

Invoke the `ckit-command-status` skill and follow it to completion. This wrapper is only the stable legacy slash-command alias; the generated skill is the behavior-bearing implementation.
