---
description: Abort the in-progress /sdlc run — terminate its authoritative snapshot and clean its worktrees
argument-hint: '[reason]'
allowed-tools: Skill
---

Invoke the `ckit-command-abort` skill and follow it to completion. Forward the caller's request unchanged as `$ARGUMENTS`. This wrapper is only the stable legacy slash-command alias; the generated skill is the behavior-bearing implementation.
