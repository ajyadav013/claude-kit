---
description: Run the full autonomous SDLC pipeline on a task via the orchestrator
argument-hint: <feature or task description>
allowed-tools: Skill
---

Invoke the `ckit-command-sdlc` skill and follow it to completion. Forward the caller's request unchanged as `$ARGUMENTS`. This wrapper is only the stable legacy slash-command alias; the generated skill is the behavior-bearing implementation.
