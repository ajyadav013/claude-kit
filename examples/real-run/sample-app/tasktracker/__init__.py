"""A tiny, dependency-free task tracker — the sample app for the real /sdlc run.

Standard library only, so every gate check (pytest, ruff) reproduces anywhere with
no install step. See ../README.md for how the example was produced.
"""

__version__ = "0.1.0"
