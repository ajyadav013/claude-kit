"""claude-kit — an autonomous, stack-agnostic SDLC for Claude Code.

This package distributes the claude-kit payload (rules, agents, skills, hooks, and templates)
and a CLI that scaffolds that payload into any project's ``.claude/`` directory. The same
payload is also consumed directly by Claude Code when claude-kit is installed as a plugin.
"""

__version__ = "0.2.0"
